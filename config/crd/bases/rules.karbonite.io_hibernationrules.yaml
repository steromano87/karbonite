---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: hibernationrules.rules.karbonite.io
spec:
  group: rules.karbonite.io
  names:
    kind: HibernationRule
    listKind: HibernationRuleList
    plural: hibernationrules
    singular: hibernationrule
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: HibernationRule is the Schema for the hibernationrules API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: HibernationRuleSpec defines the desired state of HibernationRule
            properties:
              dryRun:
                default: false
                type: boolean
              enabled:
                default: true
                type: boolean
              matchers:
                items:
                  properties:
                    labels:
                      additionalProperties:
                        type: string
                      type: object
                    names:
                      items:
                        type: string
                      type: array
                    namespaces:
                      items:
                        type: string
                      type: array
                    resources:
                      items:
                        type: string
                      type: array
                  type: object
                minItems: 1
                type: array
              schedules:
                items:
                  properties:
                    end:
                      type: string
                    start:
                      description: 'TODO: write regex validator to cover all the cases'
                      type: string
                  required:
                  - start
                  type: object
                minItems: 1
                type: array
            required:
            - matchers
            - schedules
            type: object
          status:
            description: HibernationRuleStatus defines the observed state of HibernationRule
            properties:
              lastModified:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
