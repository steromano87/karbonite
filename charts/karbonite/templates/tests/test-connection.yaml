apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "karbonite.fullname" . }}-test-connection"
  labels:
    {{- include "karbonite.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "karbonite.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
